// Twitter card for /about — re-exports from the OG image so the two stay in
// lockstep. Same pattern as the root app/twitter-image.tsx.
export { default, alt, size, contentType } from "./opengraph-image";
